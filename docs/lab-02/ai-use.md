# Lab 2 — AI Use and Reflection

**LLM / Agent used:** Claude Opus 4.6 / Gemini 3.6 Flash

## Selected Key Prompts (6–10)

| #   | Prompt (summarised)                                                                                                                                                                                             | What I did with the result                                                                                                                                             |
| :-- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | From this pdf and current project, make docs-lab2 folder create requirement as 6 .md file based on pdf requirement and let me review it                                                                         | Review `api-spec.md`, `specification.md`, `test.md` and `ui-spec.md` . I check correlation between all lab2 md file's content if it go along and don't conflict.       |
| 2   | Create `AGENTS.md` to summarize project context, Lab 2 requirements, and implementation history for future AI sessions, and add `.agents/` to `.gitignore`.                                                     | Verified `AGENTS.md` accurately captured project stack, database models, API specs, and issue roadmap; verified `.gitignore` ignored `.agents/`.                       |
| 3   | Implement Feature #2-2 (Schema & Seed): Create Prisma schema models/enums and write an idempotent seed script for categories, related systems, and requesters.                                                  | Generated schema with `RequesterUser`, `Category`, `RelatedSystem`, `Ticket`, and `Attachment`, applied migration `lab2_schema`, and verified seed script idempotency. |
| 4   | Implement Feature #2-3 (Reference Data APIs): Add `GET /api/requesters`, `GET /api/categories`, and `GET /api/related-systems` (with `?categoryId` filter), configure Vitest env loading, and write test suite. | Added API routes in Express, created tests `API-10`, `API-11`, `API-12` under `server/tests/lab-02/`, configured Vitest `.env` loading, and verified all tests pass.   |
| 5   | Implement Feature #2-4 (Dev Requester Selector): Create Zen Green CSS theme tokens, React RequesterContext with localStorage state persistence, Dev Requester Selection screen, App Header shell with route guard, and UI-01 unit tests. | Built Zen Green visual design tokens, `RequesterContext` state provider with local storage persistence, `RequesterSelector` component with loading/empty/error states, App shell with route guard, and verified `UI-01` component tests passing. |
| 6   | Implement Feature #2-5 (Create Ticket API + UI): Build TKT-YYYY-XXXXXX ticket number generator, POST /api/tickets endpoint with BR-10 system category validation, CreateTicket form UI component, and UNIT-01, API-01, API-02, UI-02, UI-03, UI-06 test suites. | Built ticket number generator utility, Express POST route with validation and relational checks, `<CreateTicket />` form with category-filtered systems, inline validation, Zen Green error banner with input state preservation, and verified all 6 new automated test suites passing. |
| 7   | Implement Feature #2-6 (My Tickets Dashboard API + UI): Build GET /api/tickets endpoint with requester ownership isolation, search, filtering, and pagination, plus MyTickets React dashboard component with desktop table and mobile card view. | Implemented Express GET tickets handler with `x-requester-id` ownership isolation (BR-05), keyword search, category/priority/status filters, default `createdAt` desc sorting, `<MyTickets />` component with desktop interactive table & mobile card view (AC-09), and verified API-03, API-04, UI-05, RESP-01 passing. |
| 8   | Review and resolve PR feedback on Feature #2-6: Audit claims regarding RESP-01 mobile simulation / touch targets >=44px, §13.1 empty loop assertion hazard, deterministic secondary sorting, and UI failure state testing. | Verified all 4 review critiques as true; added mobile viewport simulation (<768px), touch target >=44px checks, and overflow prevention in `MyTickets.test.tsx`; enforced `expect(res.body.data.length).toBeGreaterThan(0)` per §13.1 in `my-tickets.api.test.ts`; implemented deterministic secondary sort `{ id: "desc" }` in `app.ts` with test; added UI fetch error alert and dismissal test in `MyTickets.test.tsx`. |
| 9   | Implement Feature #2-7 (Ticket Detail View API + UI): Build GET /api/tickets/:id endpoint with ownership isolation (BR-18, API-05), and RequesterTicketDetail component with breadcrumb, metadata grid, and attachments display (UI-07). | Built Express GET /api/tickets/:id endpoint enforcing ownership isolation (403 Forbidden / 404 Not Found), added API-05 integration tests, built `<RequesterTicketDetail />` component with breadcrumb navigation, read-only metadata grid, and attachments display, wired into App.tsx, and verified UI-07 component tests passing. |
| 10  | Audit and resolve review finding: "The `tests.md` file's header indicates that there are 12 passable test IDs, but the actual list shows 16 IDs." | Audited `tests.md` Section 6 and Section 2; confirmed exactly 16 tests are passing and 9 pending out of 25 total; corrected header count from 12 to 16 and added explicit count of 9 pending tests for complete clarity. |
| 11  | Implement Feature #2-8 (Attachments Lifecycle): Build upload, download, and soft-remove REST APIs, file validator (BR-12/13), AttachmentSection UI with soft-removal modal dialog (UI-04), and test suites UNIT-02, API-06, API-07, API-08, API-09, UI-04. | Configured multer with timestamp sanitization; implemented `POST /api/tickets/:id/attachments` with ownership check & max 5 active limit, `GET /api/attachments/:id/download` with 410 on removed files, `DELETE /api/attachments/:id` with reason validation; built `<AttachmentSection />` with file pre-validation, download trigger, and accessible Soft-Removal Modal requiring >=3 characters; verified all 6 new test suites pass. |
| 12  | Implement Feature #2-9 (Responsive Layout, Polish & E2E Testing): Add WCAG AA focus rings, notification banner transitions (slide-in 200ms ease-out, fade-out 150ms ease-in), responsive touch targets (>=44px), Playwright E2E test suite for E2E-01, E2E-02, E2E-03, and capture responsive screenshots across Desktop, Tablet, and Mobile viewports. | Enhanced `client/src/index.css` with WCAG AA focus rings (`2px solid #0B7A46`), banner slide-in/fade-out keyframes (`ui-spec.md §4.6`), and responsive mobile touch target constraints; configured Playwright at root with `playwright.config.ts`; implemented end-to-end user journeys in `e2e/lab-02/requester-ticket-flow.spec.ts` (E2E-01, E2E-02, E2E-03 all passing); automated screenshot evidence capture across 3 viewports in `artifacts/lab-02/screenshots/`; brought overall Lab 2 test coverage to 25/25 passing (100%). |



---

## My Reflection

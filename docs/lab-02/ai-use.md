# Lab 2 — AI Use and Reflection

**LLM / Agent used:** Claude Opus 4.6 / Gemini 3.6 Flash(High) / Gemini 3.8 Flash(High)

## Selected Key Prompts (6–10)

| #   | Prompt (summarised)                                                                                                                                                                                                                                                                                                                                     | What I did with the result                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| :-- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | From this pdf and current project, make docs-lab2 folder create requirement as 6 .md file based on pdf requirement and let me review it                                                                                                                                                                                                                 | ให้ Agent สร้างไฟล์ doc ทั้งหมดของ lab2 จากการอ่าน Labsheet2 และข้อมูลไฟล์ที่ได้ดึงจาก lab1 จากนั้นทำการตรวจสอบ requirement ว่าตรงกับที่ lab2 กำหนดหรือไม่                                                                                                                                                                                                                                                                                                                         |
| 2   | Create `AGENTS.md` to summarize project context, Lab 2 requirements, and implementation history for future AI sessions, and add `.agents/` to `.gitignore`.                                                                                                                                                                                             | ได้สั่งให้ Agent สร้างไฟล์ AGENTS.md เพื่อกำหนดขอบเขตงานจนสรุปได้มาว่าจะแบ่งการทำงานเป็น 9 issues และยังให้ไฟล์ AGENTS.md เป็นตัว recap, check progress และเป็นตัวบันทึก log ของการทำงาน ทำให้สามารถใช้ไฟล์นี้สรุปสถานะของ project นี้ได้อย่างรวดเร็ว                                                                                                                                                                                                                              |
| 3   | Implement Feature #2-2 (Schema & Seed): Create Prisma schema models/enums and write an idempotent seed script for categories, related systems, and requesters.                                                                                                                                                                                          | จากที่ Agent สร้าง Schema ของ RequesterUser, Category, RelatedSystem, Ticket และ Attachment ได้มีการตรวจสอบความเรียบร้อยและได้ test seed script แล้วว่า idempotent                                                                                                                                                                                                                                                                                                                 |
| 4   | Implement Feature #2-3 (Reference Data APIs): Add `GET /api/requesters`, `GET /api/categories`, and `GET /api/related-systems` (with `?categoryId` filter), configure Vitest env loading, and write test suite.                                                                                                                                         | ตรวจสอบ test สำหรับ API-10, API-11, API-12 ใน server/tests/lab-02/ และตรวจสอบว่าเทสผ่านทั้งหมดมั้ย                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | Implement Feature #2-4 (Dev Requester Selector): Create Zen Green CSS theme tokens, React RequesterContext with localStorage state persistence, Dev Requester Selection screen, App Header shell with route guard, and UI-01 unit tests.                                                                                                                | เช็คว่า RequesterContext state provider เก็บข้อมูลลง local storage ได้จริง, ตรวจว่า Component RequesterSelector มีการจัดการสถานะ loading/empty/error และ App shell ที่มี route guard และตรวจสอบแล้วว่ารันเทสของ UI-01 component ผ่านมั้ย                                                                                                                                                                                                                                           |
| 6   | Implement Feature #2-5 (Create Ticket API + UI): Build TKT-YYYY-XXXXXX ticket number generator, POST /api/tickets endpoint with BR-10 system category validation, CreateTicket form UI component, and UNIT-01, API-01, API-02, UI-02, UI-03, UI-06 test suites.                                                                                         | ทำการตรวจสอบ ได้ test ตัว Utility สร้างหมายเลข Ticket และ Express POST route เรื่อง Validation กับ Relational checks ทดสอบฟอร์ม `<CreateTicket/>` ว่ากรอง System ตาม Category ได้และมี Inline validation รวมถึงเช็ค Error banner ว่าแสดงผลถูกต้องและจำข้อมูลที่กรอกไว้ได้ และเช็ค Automated test suites ตัวใหม่ทั้ง 6 ตัว                                                                                                                                                            |
| 7   | Implement Feature #2-6 (My Tickets Dashboard API + UI): Build GET /api/tickets endpoint with requester ownership isolation, search, filtering, and pagination, plus MyTickets React dashboard component with desktop table and mobile card view.                                                                                                        | test ตัว Express GET tickets ว่าจัดการสิทธิ์ ownership isolation ผ่าน x-requester-id (BR-05) ได้ถูกต้อง, ทดสอบระบบค้นหาด้วย keyword, ตัวกรอง และเช็คว่าเรียงข้อมูลแบบ createdAt เป็นค่าเริ่มต้น รวมถึง test หน้า `<MyTickets/>` (AC-09) ว่าบน Desktop โชว์เป็นตารางใช้งานได้จริง ส่วนบนจอมือถือสลับเป็น card view ถูกต้อง สุดท้ายได้ตรวจสอบเทส API-03, API-04, UI-05, RESP-01                                                                                                        |
| 8   | Implement Feature #2-7 (Ticket Detail View API + UI): Build GET /api/tickets/:id endpoint with ownership isolation (BR-18, API-05), and RequesterTicketDetail component with breadcrumb, metadata grid, and attachments display (UI-07).                                                                                                                | test API GET /api/tickets/:id ว่าระบบจำกัดสิทธิ์ (ownership isolation) ทำงานถูกต้อง สามารถดัก case 403 Forbidden และ 404 Not Found ได้ตามเงื่อนไข และเช็คการแสดงผลหน้า `<RequesterTicketDetail/>` ว่ามีระบบนำทางแบบ breadcrumb, โชว์ข้อมูล metadata แบบ read-only รวมถึงแสดงไฟล์แนบได้ครบถ้วน ตรวจสอบการเชื่อมต่อ routing ใน App.tsx เรียบร้อย สุดท้ายได้เช็คเทส API-05 กับ UI-07 ว่ารันผ่าน                                                                                         |
| 9   | Implement Feature #2-8 (Attachments Lifecycle): Build upload, download, and soft-remove REST APIs, file validator (BR-12/13), AttachmentSection UI with soft-removal modal dialog (UI-04), and test suites UNIT-02, API-06, API-07, API-08, API-09, UI-04.                                                                                              | ตรวจสอบระบบจัดการไฟล์แนบ ทำการ test การตั้งค่า multer เรื่อง timestamp sanitization ลองยิง API POST อัปโหลดไฟล์ว่าเช็คสิทธิ์และลิมิตไว้ที่ไม่เกิน 5 ไฟล์ได้จริง, API GET ดาวน์โหลดก็คืนค่า 410 (Gone) สำหรับไฟล์ที่ลบไปแล้ว และ API DELETE มีการบังคับ validate เหตุผลการลบ ในส่วนของหน้าจอได้เช็ค `<AttachmentSection/>` ทั้งระบบ pre-validation ก่อนอัปโหลด, ปุ่มกดโหลดไฟล์ และตัว Modal สำหรับลบไฟล์แบบ Soft-Removal ว่าทำงานได้สมบูรณ์ สุดท้ายตรวจสอบ test suites ใหม่ทั้ง 6 ตัว |
| 10  | Implement Feature #2-9 (Responsive Layout, Polish & E2E Testing): Add WCAG AA focus rings, notification banner transitions (slide-in 200ms ease-out, fade-out 150ms ease-in), responsive touch targets (>=44px), Playwright E2E test suite for E2E-01, E2E-02, E2E-03, and capture responsive screenshots across Desktop, Tablet, and Mobile viewports. | ตรวจสอบส่วน UI และ E2E เช็คไฟล์ index.css ว่ามีการตั้งค่า Focus rings ตามมาตรฐาน WCAG AA, อนิเมชันแบนเนอร์ ทำงานตรงตามสเปค และ Touch target บนมือถือรองรับ responsive ถูกต้อง จากนั้นได้ตรวจสอบการตั้งค่า Playwright และรันเทส End-to-End โฟลว์การใช้งานจริง (E2E-01 ถึง 03) ว่ารันผ่านสมบูรณ์ พร้อมทั้งเช็คระบบแคปหน้าจอเก็บหลักฐานอัตโนมัติว่าทำงานครบทั้ง 3 ขนาดหน้าจอ (viewports) และสรุปรวม test ของ Lab 2 ว่ารันผ่านครบทั้ง 25                                               |

---

## My Reflection

สิ่งที่ได้เรียนรู้จากการทำ Lab2 คือความสำคัญของการจัดการ scope งานและทำไฟล์โน้ตสถานะของงาน (AGENT.md) หลังการทำได้ทำการจัดการแบ่ง issue ย่อยและกำหนดขอบเขตย่อยของแต่ละ issue พบว่าสามารถทำงานได้อย่างราบรื่น และสามารถจัดการงานได้อย่างเป็นระบบมากขึ้น สามารถเช็คได้ง่ายว่ามีส่วนไหนขาดหรือเกินใน issue นั้นๆ อีกทั้งไฟล์ AGENT.md ยังทำให้เมื่อเริ่มแชทใหม่กับ ai agent กรณีที่ context เหลือน้อยสามารถทำได้อย่างง่ายดายไร้ปัญหาทำให้ใช้งาน agent ทำงานได้อย่างมีประสิทธิภาพดียิ่งขึ้น
